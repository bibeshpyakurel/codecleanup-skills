# Orders fixture

A private package the inventory check scans. It is not a runnable product. `CLEANUP.md` protects `examples/**`, so a cleanup of this repository leaves the fixture in place.

What the signals are here:

- `left-pad` is declared and unused
- `console.log` in `src/tax.js` is a debug leftover
- the `roundHalfUp` comment is commented-out code
- `src/tax.js.bak` is a backup file
